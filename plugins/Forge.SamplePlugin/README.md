# Stream Notes sample plugin

This package demonstrates Forge's portable manifest and declarative UI format. It contains no host-specific executable code, so another host such as Crimson can render the same schema.
